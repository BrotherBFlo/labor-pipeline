# Copy this file to .env and paste your real key after the = sign.
# .env is gitignored and will never be committed.
FRED_API_KEY=

# Optional: a GitHub token raises the unauthenticated rate limit for Indeed
# raw-file pulls. Not required — leave blank to use anonymous access.
GITHUB_TOKEN=
